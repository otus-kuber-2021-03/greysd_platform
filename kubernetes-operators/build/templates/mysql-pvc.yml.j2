apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}-pvc"
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
  volumeName: "{{ name }}-pv"
  storageClassName: standard
  selector:
    matchLabels:
      pv-usage: {{ name }}
